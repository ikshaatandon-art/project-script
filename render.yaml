services:
  - type: web
    name: project-script
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 app:app
    autoDeploy: true
